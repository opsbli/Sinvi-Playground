## Tasks

- [x] 增加 store 层回归测试，覆盖已有非默认 agent 时补齐默认 agents。
- [x] 增加重复启动回归测试，覆盖默认 agents 不倍增。
- [x] 增加历史重复演示 workflows 去重测试，并确认未知 workflow 不被删除。
- [x] 修改 `seed_defaults()` 为默认 agents 幂等补齐与保守去重。
- [x] 对本地已污染数据库执行同一路径修复，并记录前后数量。
- [x] 运行 targeted tests 和 OpenSpec artifact consistency review。
