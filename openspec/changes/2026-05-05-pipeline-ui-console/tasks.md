## Tasks

- [x] 增加 pipeline action API 测试，覆盖 bootstrap 幂等、brief 生成、sequential 执行。
- [x] 实现后端 bootstrap、PRD/story generation、sequential execute endpoints。
- [x] 增加 `frontend/src/api.js` pipeline API 封装。
- [x] 新增 `PipelinesPage.vue`，支持 definitions、brief、story queue、run detail、artifacts。
- [x] 在 `App.vue` 增加 `Pipelines` 导航、状态加载和事件处理。
- [x] 运行后端测试、前端构建和 OpenSpec strict validate。
